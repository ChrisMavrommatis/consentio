{
	"type": "SELECT",
	"name": "cookies",
	"displayName": "Cookie table",
	"macrosInSelect": true,
	"selectItems": [
		{
			"value": "none",
			"displayValue": "None"
		}
	],
	"simpleValueType": true,
	"help": "A variable holding the cookies your site sets - the JSON text of your consentio-cookies.json in a Constant, or any variable returning the same rows. Left at None, the tag reads window.ConsentioCookies from the page if the site puts it there; otherwise the settings panel shows no cookie table."
}
